# 2026-08-15 工作日志

## cmt 项目:gmt 子命令化重构

- 背景:用户不希望打包产出 cmt + gmt 两个二进制(同代码重复编译),选定方案 B(纯子命令)。
- 改动:
  - `cmd/cmt/main.go`:删除 argv[0] 分发,直接 executeCMT()
  - `cmd/cmt/cmt.go`:根命令注册 `sync <target-branch>` 子命令;根命令执行错误时用 errors.As 识别 MergeConflictError 并输出手动合并指引
  - `cmd/cmt/gmt.go`:删除 executeGMT,runGMT 改名 runSync,保留 printMergeConflictInstructions
  - `Makefile`:删除 GMT_APP_NAME、gmt 编译目标/符号链接/打包项,每平台只产 cmt 单文件
  - `README.md`:gmt test → cmt sync test,删除别名说明
- 验证:go vet / go build 通过;cmt --help 显示 sync 子命令;非 git 目录报错退出码 1;bin/ 旧 gmt 链接已清理,只剩 cmt。
- 遗留(未处理):client.go 的 Anthropic Content[1] 越界 P0 bug 仍在,建议后续修复。

## cmt 项目:工具改名为 qg,子命令恢复为 gmt

- 用户进一步调整命令形态:根命令 `cmt` → `qg`,子命令 `sync` → `gmt`(即 `qg gmt <目标分支名>`)。
- 改动:
  - `cmd/cmt/cmt.go`:rootCmd.Use 改为 "qg";syncCmd 改名 gmtCmd,Use 为 "gmt <target-branch>"
  - `cmd/cmt/gmt.go`:runSync 改名 runGMT
  - `Makefile`:QG_APP_NAME = qg,所有产物/打包名 cmt-* → qg-*
  - `README.md`:全量重写命令引用,修正 Go install 段为 `go install github.com/zfslalala/cmt/cmd/cmt@latest` + mv 重命名
- 验证:bin/ 只有 qg 一个二进制;qg --help 与 qg gmt --help 输出正确;非 git 目录退出码 1。
- 最终命令形态:`qg`(生成提交信息)/ `qg gmt <branch>`(分支同步)。

## cmt 项目:cmt 也改为 qg 子命令,前缀统一

- 用户要求前缀统一 `qg`:`cmt` 从根命令默认行为改为 `qg cmt` 子命令。
- 改动:
  - `cmd/cmt/cmt.go`:rootCmd 移除 RunE(无参数显示帮助);新增 cmtCmd 子命令(Use "cmt",挂载 -v/-e/-p flags);gmtCmd 保持
  - `README.md`:使用示例全部改为 `qg cmt [flags]`,说明 `qg` 直接运行显示帮助
- 最终命令形态:`qg cmt`(生成提交信息,含 -v/-e/-p)/ `qg gmt <branch>`(分支同步)/ `qg`(帮助)。
- 验证:qg --help 列出 cmt/gmt;各命令退出码正确(qg=0,cmt/gmt 错误=1)。

## cmt 项目:代码结构整理 + README 优化

- 代码结构:
  - 新增 `cmd/cmt/root.go`:命令树注册(executeRoot)+ returnCommandError(原 errors.go 内容并入)
  - `cmd/cmt/cmt.go` 只保留 runCMT(提交信息生成);`gmt.go` 只保留 runGMT + 冲突指引
  - 删除 `cmd/cmt/errors.go`;main.go 调 executeRoot
  - 清理根目录遗留旧 cmt 二进制;.gitignore 增加 /cmt /qg
  - 文件职责 = 子命令:root.go/cmd 树、cmt.go/cmt、gmt.go/gmt
- README 优化:
  - clone/releases URL 修正为 github.com/zfslalala/cmt(原来写 z/cmt 与 remote 不符)
  - 新增"命令结构"与"项目结构"小节
  - 配置项表格化(CMT_API_KEY/URL/MODEL,标注必填与默认值)
  - 预编译安装说明与 Makefile 产物文件名对齐(qg-darwin-arm64.tar.gz 等)
  - 分支同步说明精简为要点式
- 验证:go vet + make build 通过;qg/cmt/gmt 三个命令 help 输出正确;bin/ 仅 qg。
- 代码规模:约 900 行,10 个 Go 文件。

## cmt 项目:安装 qg 到用户 PATH

- `make install PREFIX=$HOME/.local` 安装 qg 到 ~/.local/bin(免 sudo)。
- ~/.zshrc 第 29 行 PATH 增加 `$HOME/.local/bin`(置于最前)。
- 验证:which qg → /Users/z/.local/bin/qg,命令可用。
- 注意:用户尚未配置 CMT_API_KEY(~/.cmt.env 与项目 .env 均不存在),首次使用需先配置。

## cmt 项目:cmd 布局调整(用户偏好 cmd/ 直接为 main 包)

- 用户偏好:cmd 目录本身作为 package main,main.go 直接放 cmd/ 下(而非 cmd/cmt/)。
- 改动:git mv cmd/cmt/{main,root,cmt,gmt}.go → cmd/;删除空目录 cmd/cmt。
- 同步更新:Makefile 构建路径 ./cmd/cmt → ./cmd;README 中 go install 路径改为 .../cmd@latest、Windows 编译 ./cmd、项目结构小节。
- 验证:go vet + go build ./cmd 通过,命令行为正常;已重新 make install 到 ~/.local/bin。
- 布局偏好(用户约定):cmd/ 下放 main.go(入口,package main),cmd/cmt/ 是子包(package cmt,命令实现,不含 main)——不是 cmd/<app>/{main.go} 多层,也不是 cmd/ 全平铺。

## cmt 项目:项目定位改为 Quick Git

- 用户定位:QG = Quick Git,快速 Git 操作工具;AI 作为可选增强(qg cmt 生成 commit message 即 AI 整合的例子);纯 git 操作(如 qg gmt)无需配置。
- 改动:
  - `cmd/cmt/root.go`:rootCmd Short/Long 改为 "Quick Git:快速 Git 操作工具" / "快速 Git 操作工具,支持 AI 自动生成 commit message,兼容所有 OpenAI 协议的大模型服务。"
  - `README.md`:标题改为 QG - Quick Git,简介/功能特性/命令结构/配置章节重写(配置标注"仅 qg cmt 需要",gmt 标注"无需配置")
- 意外:发现 cmd/cmt/main.go 残留旧文件(内容引用已删除的 executeRoot,package main,与 package cmt 冲突,来源疑似 IDE 操作),已 git rm。
- 验证:go vet + make install 通过;qg --help 显示新定位描述。






